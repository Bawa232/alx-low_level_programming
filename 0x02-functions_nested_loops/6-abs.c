#include "main.h"

/**
 *_abs - function to return absolute value
 * @a: An integre input
 * Return: Absolut value of number r
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (a * -1);
}
